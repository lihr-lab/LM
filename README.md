# LM - Log-based Anomaly Detection for WAF

An unsupervised anomaly detection system for Web Application Firewall (WAF) logs.

## Features
- URI normalization with `{NUM}`, `{UUID}`, `{B64}`
- Multi-dimensional feature extraction
- Supports Isolation Forest and One-Class SVM

## Usage
python src/features.py